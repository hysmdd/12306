# nacos server地址
spring:
  application:
    # 注册中心的名字
    name: member
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
  # 启动环境，nacos会根据环境读不通的配置
  profiles:
    active: dev